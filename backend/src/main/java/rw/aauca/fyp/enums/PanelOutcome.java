package rw.aauca.fyp.enums;

public enum PanelOutcome {
    CLEARED,   // pre-defense only
    PASSED,    // defense only
    REFERRED,  // defense only — needs revision
    FAILED     // defense only
}
